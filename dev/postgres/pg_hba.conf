# Passwordless authentication for ObzenFlow's loopback-published development
# service. This policy is deliberately unsuitable for shared or production
# hosts. The Compose file is the network boundary and must publish only on
# 127.0.0.1.
local all all trust
host all all all trust
